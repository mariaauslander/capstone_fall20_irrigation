from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A model used to detect irrigation, valuing freshwater as a resource.',
    author='Berkeley-Data',
    license='MIT',
)
